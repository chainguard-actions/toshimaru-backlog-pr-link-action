name: Test no pull_request payload error
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      # The action requires a pull_request event context. Under workflow_dispatch
      # (used by act), context.payload.pull_request is undefined, so the action
      # should fail with a specific error message.
      - id: action
        uses: .
        with:
          backlog-api-key: fake-api-key
          backlog-host: example.backlog.com
        continue-on-error: true

      - name: Assert action failed with expected error
        env:
          OUTCOME: ${{ steps.action.outcome }}
        run: |
          echo "outcome: $OUTCOME"
          if [ "$OUTCOME" != "failure" ]; then
            echo "FAIL: expected action to fail when no pull_request payload is present, but outcome was '$OUTCOME'"
            exit 1
          fi
          echo "PASS: action correctly failed when triggered outside of a pull_request event"
